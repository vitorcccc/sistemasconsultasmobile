/**
 * =============================================================================
 * ESTILOS: MinhasConsultas
 * =============================================================================
 * 
 * Responsável: Victoria Moura (RM555474)
 * Tela: MinhasConsultas
 * 
 * Estilos para a tela de minhas consultas do paciente
 * 
 * =============================================================================
 */

import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#f5f5f5",
  },

  header: {
    padding: 20,
    alignItems: "center",
  },

  titulo: {
    fontSize: 24,
    fontWeight: "bold",
    color: "#2196F3",
    marginBottom: 8,
  },

  subtitulo: {
    fontSize: 14,
    color: "#666",
  },
});

export default styles;